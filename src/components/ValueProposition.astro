---

---

<section
    class="py-16 bg-slate-900 text-white relative overflow-hidden"
    id="value-prop"
>
    <!-- Abstract blur -->
    <div
        class="absolute -top-20 -left-20 w-80 h-80 bg-blue-600/30 rounded-full blur-[100px]"
    >
    </div>

    <div class="container mx-auto px-4 relative z-10">
        <h2
            class="text-3xl md:text-4xl font-bold text-center mb-12 opacity-0 value-header"
        >
            Tu tranquilidad es nuestra prioridad
        </h2>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
            <!-- Feature 1 -->
            <div
                class="bg-white/5 backdrop-blur-sm border border-white/10 p-6 rounded-xl hover:bg-white/10 transition-colors value-card"
            >
                <div class="text-4xl mb-4">🚀</div>
                <h3 class="font-bold text-xl mb-2">Rapidez</h3>
                <p class="text-slate-300 text-sm">
                    Agendamiento express. Vamos a tu casa en 24 hrs.
                </p>
            </div>

            <!-- Feature 2 -->
            <div
                class="bg-white/5 backdrop-blur-sm border border-white/10 p-6 rounded-xl hover:bg-white/10 transition-colors value-card"
            >
                <div class="text-4xl mb-4">🌍</div>
                <h3 class="font-bold text-xl mb-2">Cobertura Total</h3>
                <p class="text-slate-300 text-sm">
                    La Serena, Coquimbo, Ovalle, Tongoy, Guanaqueros, Vicuña,
                    Paihuano.
                </p>
            </div>

            <!-- Feature 3 -->
            <div
                class="bg-white/5 backdrop-blur-sm border border-white/10 p-6 rounded-xl hover:bg-white/10 transition-colors value-card"
            >
                <div class="text-4xl mb-4">🤝</div>
                <h3 class="font-bold text-xl mb-2">Transparencia</h3>
                <p class="text-slate-300 text-sm">
                    Presupuesto claro después del diagnóstico. Sin cobros
                    ocultos.
                </p>
            </div>

            <!-- Feature 4 -->
            <div
                class="bg-white/5 backdrop-blur-sm border border-white/10 p-6 rounded-xl hover:bg-white/10 transition-colors value-card"
            >
                <div class="text-4xl mb-4">🛡️</div>
                <h3 class="font-bold text-xl mb-2">Garantía</h3>
                <p class="text-slate-300 text-sm">
                    Respaldo técnico en cada reparación realizada.
                </p>
            </div>
        </div>
    </div>
</section>

<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    const tl = gsap.timeline({
        scrollTrigger: {
            trigger: "#value-prop",
            start: "top 75%",
        },
    });

    tl.to(".value-header", { opacity: 1, duration: 0.5 }).to(
        ".value-card",
        {
            opacity: 1,
            y: 0,
            duration: 0.6,
            stagger: 0.15,
            ease: "back.out(1.2)",
        },
        "-=0.2",
    );
</script>
