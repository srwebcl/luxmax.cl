---
const brands = [
    "Samsung", "LG", "Fensa", "Mademsa", "Whirlpool", "Daewoo", "Bosch",
    "Electrolux", "Midea", "Ursus Trotter", "Fensa", "General Electric",
    "Samsung", "LG", "Fensa", "Mademsa", "Whirlpool", "Daewoo", "Bosch", // Duplicate for infinite loop illusion
];

const brandCards = [
    {
        title: "Samsung & LG",
        desc: "Especialistas en sistemas Inverter, carga frontal y fallas de tarjeta electrónica.",
        img: "/images/samsung.webp",
        brands: ["Samsung", "LG"]
    },
    {
        title: "Mademsa & Fensa",
        desc: "Usamos repuestos originales para las reparaciones.",
        img: "/images/mademsa.webp",
        brands: ["Mademsa", "Fensa"]
    },
    {
        title: "Whirlpool & Americana",
        desc: "Soluciones técnicas para equipos de alto rendimiento y gran capacidad.",
        img: "/images/whirpool.webp",
        brands: ["Whirlpool", "General Electric"]
    }
];
---

<section class="py-20 bg-slate-50 relative overflow-hidden" id="brands">
    <div class="container mx-auto px-4 relative z-10">
        <!-- Section Header -->
        <div class="text-center mb-16 space-y-4 max-w-3xl mx-auto">
            <h2 class="text-3xl md:text-5xl font-black text-slate-800 tracking-tight">
                Marcas que <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-cyan-500">Reparamos</span>
            </h2>
            <p class="text-lg text-slate-600 font-medium">
                Conocemos los códigos de error y diagramas exactos de los líderes del mercado.
            </p>
        </div>

        <!-- Infinite Ticker -->
        <div class="w-full overflow-hidden mb-20 relative mask-linear-fade">
            <div class="flex gap-12 md:gap-20 w-max animate-ticker">
                {brands.map((brand) => (
                    <span class="text-4xl md:text-6xl font-bold text-slate-300 whitespace-nowrap select-none hover:text-blue-500/20 transition-colors">
                        {brand}
                    </span>
                ))}
            </div>
        </div>

        <!-- Enhanced Cards Grid -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {brandCards.map((card) => (
                <div class="group brand-card relative bg-white rounded-3xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-500 hover:-translate-y-2 border border-slate-100">
                    <!-- Image Placeholder Area -->
                    <div class="h-48 bg-slate-100 relative overflow-hidden">
                        <img 
                            src={card.img} 
                            alt={card.title} 
                            class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                            loading="lazy"
                        />
                        <div class="absolute inset-0 bg-gradient-to-t from-white/90 via-transparent to-transparent"></div>
                    </div>
                    
                    <!-- Content -->
                    <div class="p-8 relative">
                        <div class="absolute -top-10 right-6 bg-blue-600 text-white p-3 rounded-2xl shadow-xl shadow-blue-500/30 group-hover:scale-110 transition-transform">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                        </div>
                        <h3 class="text-2xl font-bold text-slate-800 mb-3 group-hover:text-blue-600 transition-colors">
                            {card.title}
                        </h3>
                        <p class="text-slate-600 leading-relaxed mb-4">
                            {card.desc}
                        </p>
                        <div class="flex gap-2 flex-wrap">
                            {card.brands.map(b => (
                                <span class="px-3 py-1 bg-slate-100 text-slate-600 text-xs font-bold rounded-full uppercase tracking-wide">
                                    {b}
                                </span>
                            ))}
                        </div>
                    </div>
                </div>
            ))}
        </div>

        <!-- High Impact CTA Banner -->
        <div class="mt-20 relative group brand-cta">
            <div class="absolute inset-0 bg-gradient-to-r from-blue-900 to-slate-900 rounded-3xl transform rotate-1 group-hover:rotate-0 transition-transform duration-500 opacity-90"></div>
            <div class="relative bg-white rounded-3xl p-8 md:p-12 border border-slate-200 shadow-2xl overflow-hidden flex flex-col md:flex-row items-center justify-between gap-8 group-hover:-translate-y-1 transition-transform duration-500">
                <!-- Decorative Elements -->
                <div class="absolute -right-20 -top-20 w-64 h-64 bg-blue-500/10 rounded-full blur-3xl"></div>
                
                <div class="space-y-4 text-center md:text-left z-10">
                    <h3 class="text-3xl md:text-4xl font-black text-slate-800">
                        ¿No encuentras tu <span class="text-blue-600">Marca</span>?
                    </h3>
                    <p class="text-lg text-slate-600 max-w-xl">
                        Nuestros técnicos multimarca están capacitados para diagnosticar equipos de fabricantes internacionales menos comunes. ¡Consúltanos!
                    </p>
                </div>

                <div class="z-10 w-full md:w-auto">
                    <a 
                        href="https://wa.me/56967140558?text=Hola%2C%20tengo%20una%20marca%20de%20electrodom%C3%A9stico%20espec%C3%ADfica%20y%20necesito%20saber%20si%20lo%20reparan."
                        target="_blank"
                        class="block w-full md:inline-block text-center bg-green-500 hover:bg-green-600 text-white text-xl font-bold py-4 px-10 rounded-xl shadow-lg shadow-green-500/30 transition-all hover:shadow-green-500/50 hover:scale-105"
                    >
                        Solicitar Presupuesto
                    </a>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .mask-linear-fade {
        mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent);
    }

    @keyframes ticker {
        0% { transform: translateX(0); }
        100% { transform: translateX(-50%); }
    }

    .animate-ticker {
        animation: ticker 25s linear infinite;
        width: fit-content;
    }
    
    .animate-ticker:hover {
        animation-play-state: paused;
    }
</style>

<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";
    
    gsap.registerPlugin(ScrollTrigger);

    // Fade In Elements on Scroll - Target specific elements
    const cards = document.querySelectorAll('.brand-card');
    const cta = document.querySelector('.brand-cta');
    
    // Animate Cards - Use FROM logic for default visibility fail-safe
    if(cards.length > 0) {
        gsap.from(cards, {
            scrollTrigger: {
                trigger: "#brands",
                start: "top 75%",
                toggleActions: "play none none reverse"
            },
            y: 50,
            opacity: 0,
            duration: 0.8,
            stagger: 0.15,
            ease: "power3.out",
            clearProps: "all" // Ensure clean state after animation
        });
    }

    // Animate CTA
    if(cta) {
        gsap.from(cta, {
            scrollTrigger: {
                trigger: "#brands",
                start: "top 70%"
            },
            y: 30,
            opacity: 0,
            duration: 0.8,
            delay: 0.2,
            ease: "power3.out",
            clearProps: "all"
        });
    }
</script>
